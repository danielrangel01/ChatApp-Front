import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  content: {
    padding: 20,
  },
  input: {
    backgroundColor: "#29292b",
    color: "#fff",
    fontSize: 16,
    borderRadius: 10,
  },
});
